spring:
  flyway:
    enabled: false
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: always
      data-locations: classpath:db/data.sql

looky:
  result-generation:
    s3:
      bucket: app-contents-test
      region: ap-northeast-2
